# 漫剧 Agent 客户端重构计划

## 产品定义

这是一个参考 ChatGPT/Codex 信息架构的桌面 Agent 客户端。Agent 专门负责小说到漫画、再到漫剧的生产过程。

客户端本身不是带演示数据的后台，也不是传统剪辑器。所有项目、对话、文件、角色、分镜和成片都必须来自用户真实操作或真实任务结果。

## 固定信息架构

1. 左侧：项目、对话和一级功能入口。
2. 中间：Agent 对话或模型配置等一级页面。
3. 右侧：当前项目的真实文件和任务资源。

## 必须满足

- 默认启动为空状态，不预置任何小说、项目、角色、分镜、任务或成片。
- 未配置对话模型时禁止生成假回复。
- 模型配置是左侧可直接进入的一级页面。
- 对话、生图、视频模型分别配置。
- 模型配置支持 Base URL、API Key、模型 ID、自定义请求路径、自定义请求头、启用状态和连接测试。
- 导入真实小说后才创建项目资源。
- 右侧栏收起时宽度从 360px 动画过渡到 0，中间区域同步扩展。
- 应用高度始终限制在窗口内，不产生页面级滚动。
- 左侧列表、中间对话和右侧资源分别独立滚动。
- 底部输入框始终位于窗口可视区域。
- Tauri 窗口必须支持拖动、最小化、最大化和关闭。
- 项目、对话、资源和模型配置保存在本机。
- React/Vite 界面修改支持热更新。
- 输入框、文本域、下拉框、弹窗、通知和错误提示必须使用统一的自定义 UI 组件与设计令牌。
- 禁止直接显示浏览器或系统原生下拉框、弹窗、错误提示样式。
- 页面、标签、侧栏、资源分组、下拉菜单、弹窗和通知的显示/隐藏或切换必须带有过渡动画。

## 不允许

- 不允许硬编码示例小说、示例项目、示例对话、假角色、假素材或假任务。
- 不允许在模型未配置时使用本地模拟回复冒充真实调用。
- 不允许通过 `display: none` 假装完成右侧栏收起，同时保留空白宽度。
- 不允许整个页面随文件树高度溢出窗口。
- 不允许将模型配置隐藏在难以发现的临时弹窗中。
- 不允许混用原生控件外观与客户端自定义视觉语言。
- 不允许任何界面状态无过渡地突变。

## 实施顺序

- [x] 固定需求与验收标准。
- [x] 删除旧版 Mock 数据。
- [x] 建立空项目、项目、对话、资源的数据结构。
- [x] 重写三栏容器和滚动结构。
- [x] 实现右栏折叠动画。
- [x] 实现模型配置一级页面。
- [x] 接入真实对话模型调用。
- [x] 实现真实小说导入与本地保存。
- [x] 验证窗口控制、滚动、折叠、导入、配置和对话。
- [x] 以源码组件接入轻量微交互，不安装整套动效库。
- [x] 为项目、对话和资源列表加入逐项进入动画。
- [x] 为输入区和模型配置面板加入按需聚光反馈。
- [x] 将持续动画限制在真实生成状态，并支持减少动态效果偏好。
- [x] 按全局操作、项目内容、工作区设置重构左侧信息架构。
- [x] 将模型配置收归“设置 → 模型服务”，移除左侧重复入口。
- [x] 将任务入口收归当前项目右侧栏，导入小说改为上下文操作。
- [x] 实现支持 Ctrl+K 和键盘导航的真实工作区搜索。
