# 漫剧 Agent 客户端产品与实施计划

## 产品定义

这是一个参考 ChatGPT/Codex 信息架构的桌面 Agent 客户端。核心交互是“对话即任务”，用于完成小说拆解、剧本整理、分镜设计、静态物料生成和漫剧生成。

这里没有独立的“新建项目”流程：

- 每次新对话就是一轮新任务。
- 新任务默认可以不绑定文件夹。
- 用户为当前对话选择本地文件夹后，该文件夹自动成为项目上下文。
- 绑定同一文件夹的多轮任务归组显示在该项目文件夹下。
- 右侧栏只展示当前任务所属文件夹的真实资源和生成任务。

## 固定信息架构

1. 左侧：新建任务、搜索任务、非空的置顶/项目/最近分区、底部工作区入口。
2. 中间：始终是当前任务的对话界面；输入框上方提供当前项目文件夹选择器。
3. 右侧：当前项目文件夹的真实文件、制作资源和运行任务。
4. 设置：独立模态窗口，包含模型服务、API 配置以及数据与安全。

## 左侧栏产品规则

- 左侧栏是任务导航，不是功能后台；只有真实可用的全局动作才能成为顶部入口。
- “新建任务”创建空白草稿，首次发送消息或绑定文件夹后才写入任务历史。
- “搜索任务”使用 `Ctrl/Cmd + K` 打开工作区搜索弹窗。
- “生成队列”只有在具备真实后台任务、进度和失败重试能力后才显示。
- 没有置顶任务时，整个“置顶”标题和区域隐藏。
- 置顶任务按 `pinnedAt` 降序；项目内任务置顶后仍保留在所属项目下。
- 没有项目文件夹时，整个“项目”标题和区域隐藏。
- 项目文件夹按子任务最近活动时间排序，文件夹行负责展开和折叠。
- “最近”只显示未绑定文件夹且未置顶的任务，并按 `updatedAt` 降序。
- 没有符合条件的最近任务时，整个“最近”标题和区域隐藏。
- 空侧栏不显示“暂无项目”“暂无最近任务”等说明，保留安静留白。
- 任务行悬停显示 `···` 菜单，至少支持置顶/取消置顶；所有菜单使用自定义 UI。
- 分区折叠和项目折叠状态保存在本机，切换必须有过渡动画。

## 必须满足

- 默认启动为空状态，不预置任何小说、项目、角色、分镜、任务或成片。
- 不出现“新建项目”按钮、弹窗或空项目流程。
- 对话可直接创建；选择真实文件夹后再形成项目上下文。
- 新建但尚未发送、导入或绑定文件夹的任务不得进入历史列表。
- 任务支持置顶和取消置顶，状态在重启后保持。
- 对话、生图、视频模型分别配置。
- 对话模型提供 OpenAI、Gemini、Anthropic Claude/Claude Code、智谱 GLM、Moonshot Kimi、DeepSeek、通义千问、火山方舟和自定义兼容服务预设。
- 生图模型统一使用字节火山方舟 Seedream 系列预设。
- 视频模型统一使用字节火山方舟 Seedance 系列预设。
- 模型配置支持 Base URL、API Key、模型 ID、自定义请求路径、自定义请求头、启用状态和连接测试。
- 选择供应商并填写 API Key 后，可以从供应商接口拉取当前账号可用模型并通过自定义下拉框选择，同时保留模型 ID 手动输入。
- 输入框模型入口用于直接切换已拉取或已选中的对话模型，模型设置作为菜单内的次级入口。
- 未配置对话模型时禁止生成假回复。
- 导入小说必须进入当前对话绑定的项目文件夹。
- 右侧栏收起时宽度动画过渡到 0，中间区域同步扩展。
- 右侧栏支持拖动调宽，双击分隔条恢复默认宽度。
- 应用高度始终限制在窗口内，不产生页面级滚动。
- 左侧列表、中间对话、右侧资源和设置内容分别独立滚动。
- Tauri 窗口支持拖动、最小化、最大化和关闭。
- 项目文件夹索引、任务、资源和模型配置保存在本机。
- React/Vite 界面修改支持热更新。
- 输入框、文本域、下拉框、弹窗、通知和错误提示统一使用自定义 UI。
- 页面、标签、侧栏、下拉菜单、弹窗和通知切换必须带过渡动画。

## 不允许

- 不允许硬编码演示项目、小说、对话、角色、素材或任务。
- 不允许用“项目管理后台”的信息架构替代对话式 Agent。
- 不允许在模型未配置时使用本地模拟回复。
- 不允许使用浏览器原生下拉框、弹窗或错误提示外观。
- 不允许通过隐藏内容但保留宽度来假装完成侧栏收起。
- 不允许整个页面随文件树或设置表单溢出窗口。
- 不允许渲染只有标题、没有内容的空分区。
- 不允许点击“新建任务”就持久化空白历史记录。

## 当前实施状态

- [x] 删除所有 Mock 业务数据。
- [x] 建立“任务 + 可选项目文件夹”数据模型与旧数据迁移。
- [x] 删除新建项目入口和弹窗。
- [x] 接入 Tauri 原生目录选择能力。
- [x] 左侧按项目文件夹归组任务，并单列未绑定的最近任务。
- [x] 中间固定为对话界面，项目文件夹选择器放入输入区。
- [x] 设置重做为独立模态窗口。
- [x] 对话、生图、视频模型统一收归“设置 → 模型服务”。
- [x] 增加主要对话厂商预设、Anthropic Messages 原生协议适配和字节 Seedream 生图预设。
- [x] 将视频模型限定为字节火山方舟 Seedance，并配置异步视频生成任务端点。
- [x] 增加供应商模型目录拉取、类型筛选、加载/错误状态和手动输入兜底。
- [x] 增加输入框模型切换菜单、模型目录本地记忆和设置保存后自动关闭。
- [x] 实现真实模型连接测试与对话请求。
- [x] 实现真实小说导入、本地保存和文本预览。
- [x] 实现右栏折叠动画、拖动调宽和宽度记忆。
- [x] 实现左栏拖动调宽、键盘调整、双击复位和宽度记忆。
- [x] 将“漫剧 Agent”改为带焦点管理和开合动画的 Agent 切换列表；当前仅展示真实存在的漫剧 Agent。
- [x] 实现 Ctrl+K 工作区搜索与键盘导航。
- [x] 实现统一控件视觉和状态过渡。
- [x] 新建任务改为延迟持久化的草稿。
- [x] 增加任务置顶字段、v4 数据迁移与真实置顶操作。
- [x] 空的置顶/项目/最近分区整体隐藏。
- [x] 增加侧栏分区及项目折叠状态持久化。
- [x] 增加任务行自定义操作菜单。
- [ ] 接入生图模型实际生成协议。
- [ ] 接入视频模型实际生成协议和任务轮询。
- [ ] 将 API Key 从 WebView 存储迁移到系统凭据存储。
