# 漫剧 Agent 客户端产品与实施计划

## 产品定义

这是一个参考 ChatGPT/Codex 信息架构的桌面 Agent 客户端。核心交互是“对话即任务”，用于完成小说拆解、剧本整理、分镜设计、静态物料生成和漫剧生成。

这里没有独立的“新建项目”流程：

- 每次新对话就是一轮新任务。
- 新任务默认可以不绑定文件夹。
- 用户为当前对话选择本地文件夹后，该文件夹自动成为项目上下文。
- 绑定同一文件夹的多轮任务归组显示在该项目文件夹下。
- 右侧栏只展示当前任务所属文件夹的真实资源和生成任务。

## 固定信息架构

1. 左侧：新建对话、搜索、项目文件夹分组、未绑定文件夹的最近任务、底部设置入口。
2. 中间：始终是当前任务的对话界面；输入框上方提供当前项目文件夹选择器。
3. 右侧：当前项目文件夹的真实文件、制作资源和运行任务。
4. 设置：独立模态窗口，包含模型服务、API 配置以及数据与安全。

## 必须满足

- 默认启动为空状态，不预置任何小说、项目、角色、分镜、任务或成片。
- 不出现“新建项目”按钮、弹窗或空项目流程。
- 对话可直接创建；选择真实文件夹后再形成项目上下文。
- 对话、生图、视频模型分别配置。
- 模型配置支持 Base URL、API Key、模型 ID、自定义请求路径、自定义请求头、启用状态和连接测试。
- 未配置对话模型时禁止生成假回复。
- 导入小说必须进入当前对话绑定的项目文件夹。
- 右侧栏收起时宽度动画过渡到 0，中间区域同步扩展。
- 右侧栏支持拖动调宽，双击分隔条恢复默认宽度。
- 应用高度始终限制在窗口内，不产生页面级滚动。
- 左侧列表、中间对话、右侧资源和设置内容分别独立滚动。
- Tauri 窗口支持拖动、最小化、最大化和关闭。
- 项目文件夹索引、任务、资源和模型配置保存在本机。
- React/Vite 界面修改支持热更新。
- 输入框、文本域、下拉框、弹窗、通知和错误提示统一使用自定义 UI。
- 页面、标签、侧栏、下拉菜单、弹窗和通知切换必须带过渡动画。

## 不允许

- 不允许硬编码演示项目、小说、对话、角色、素材或任务。
- 不允许用“项目管理后台”的信息架构替代对话式 Agent。
- 不允许在模型未配置时使用本地模拟回复。
- 不允许使用浏览器原生下拉框、弹窗或错误提示外观。
- 不允许通过隐藏内容但保留宽度来假装完成侧栏收起。
- 不允许整个页面随文件树或设置表单溢出窗口。

## 当前实施状态

- [x] 删除所有 Mock 业务数据。
- [x] 建立“任务 + 可选项目文件夹”数据模型与旧数据迁移。
- [x] 删除新建项目入口和弹窗。
- [x] 接入 Tauri 原生目录选择能力。
- [x] 左侧按项目文件夹归组任务，并单列未绑定的最近任务。
- [x] 中间固定为对话界面，项目文件夹选择器放入输入区。
- [x] 设置重做为独立模态窗口。
- [x] 对话、生图、视频模型统一收归“设置 → 模型服务”。
- [x] 实现真实模型连接测试与对话请求。
- [x] 实现真实小说导入、本地保存和文本预览。
- [x] 实现右栏折叠动画、拖动调宽和宽度记忆。
- [x] 实现 Ctrl+K 工作区搜索与键盘导航。
- [x] 实现统一控件视觉和状态过渡。
- [ ] 接入生图模型实际生成协议。
- [ ] 接入视频模型实际生成协议和任务轮询。
- [ ] 将 API Key 从 WebView 存储迁移到系统凭据存储。
